#Reverse a string using slicing 

s = input("Enter the string : ")

# The string is accessed from the last index,
# moving backwards by 1 position.

print(s[::-1])

#Print the last character of the string